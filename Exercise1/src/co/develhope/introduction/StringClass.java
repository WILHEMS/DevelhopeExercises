package co.develhope.introduction;

import java.util.Arrays;

public class StringClass {
    public static void main(String[] args) {
        String string1 = "Hello";
        String string2 = "How are you?";
        String string1Cut = string1.substring(0,2);
        String string2Cut = string2.substring(string2.length()-2);
        //String[] charArray = {string1.substring(0,2),string2.substring(string2.length()-2)};

        String[] charArray;

        //charArray = Arrays.copyOf(string1Cut, string1Cut.length()+1);
        //System.arraycopy(second, 0, both, first.length, second.length);

       // Arrays.cop


       // System.out.println(charArray);
    }
}
